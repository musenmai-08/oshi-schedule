# ユースケース

| ID    | 主体   | 事前条件                | 操作                         | 成功結果                                                    | 主な例外                                      |
| ----- | ------ | ----------------------- | ---------------------------- | ----------------------------------------------------------- | --------------------------------------------- |
| UC-01 | 利用者 | 招待メールである        | Google でログイン            | Supabase セッションを得る                                   | 招待外は 403                                  |
| UC-02 | 利用者 | ログイン済み            | 初回設定                     | トークンを暗号化保存し専用カレンダーを一度だけ作る          | 権限不足は再連携状態                          |
| UC-03 | 利用者 | 登録数 3 未満           | `@handle` を検索・確認・登録 | Subscription確定後に初回同期jobを受付し201を返す            | 不正handle、重複、上限、dispatch/初回同期失敗 |
| UC-04 | 利用者 | 所有する登録がある      | 一時停止/再開                | 停止中は取得・反映対象外                                    | 他人の ID は 404                              |
| UC-05 | 利用者 | 有効な登録がある        | 今すぐ同期                   | jobを受付して202、status polling後にCalendar反映            | active run再利用、cooldown 429、失敗再試行    |
| UC-06 | worker | DB 接続可能             | 定期同期                     | チャンネルごとに一度取得し利用者ごとに反映                  | 1 利用者の失敗を隔離                          |
| UC-07 | 利用者 | 登録がある              | 登録解除                     | 未来の対応イベントだけ削除し関連を外す                      | 過去イベントは維持                            |
| UC-08 | 利用者 | 再認証状態              | Google 再連携                | 新トークンを保存し同期を再開                                | refresh token 不在は再同意                    |
| UC-09 | 利用者 | `DELETE` と確認文字入力 | アカウント削除               | 永続墓石を作りCalendar、認証情報、関連、Auth userを段階削除 | 途中失敗は同じ主体で再開                      |

## 権限制約

認証主体は JWT の `sub` だけから決める。URL の subscription ID は必ず当該ユーザー所有条件と組み合わせて検索し、存在有無を漏らさない。

通常APIは既存Userだけを利用し、暗黙作成しない。初回User作成はonboardingに限定する。削除墓石がある `sub` は通常APIと再onboardingを拒否し、削除APIの再開だけを許可する。

UC-03は登録トランザクション終了後に同期UseCaseを呼ぶ。同期失敗は登録を取り消さず、`SUCCESS` / `DEFERRED` / `FAILED` を応答と履歴で区別する。初回同期は手動用quota区分を使うが、5分クールダウンの起点にはせず即時再試行を許可する。
